@using X.PagedList
@using X.PagedList.Mvc.Core
@using X.PagedList.Web.Common
@model IPagedList<CoreLibrary.Models.Publisher>
@{
    ViewData["Title"] = "Index";
}

<h1>Publisher List</h1>
<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Name</th>
        <th>Actions</th>
    </tr>
    @{
        int sNumber = 0;
    }
    @foreach (var item in Model)
    {
        sNumber++;
        <tr>
            <td>@sNumber</td>
            <td>@item.Name</td>
            <td>
                <a href="/Publisher/PublisherRemove/@item.PublisherId" class="btn btn-sm btn-danger">Remove</a>
                <a href="/Publisher/PublisherUpdate/@item.PublisherId" class="btn btn-sm btn-warning">Update</a>
                <a href="/Publisher/PublisherBooks/@item.PublisherId" class="btn btn-sm btn-primary">Books</a>
            </td>
        </tr>
    }
</table>
@Html.PagedListPager((IPagedList)Model,page => Url.Action("Index",
        new { Page = page }),
        new PagedListRenderOptions {
            LiElementClasses = new string[] { "page-item" },
            PageClasses = new string[] { "page-link" }
    })
<br />
<a href="/Publisher/PublisherAdd/" class="btn btn-primary">New Publisher</a>

